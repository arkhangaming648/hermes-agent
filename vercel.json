{
  "name": "hermes-agent",
  "version": 2,
  "builds": [
    {
      "src": "**/*.html",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "."
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}